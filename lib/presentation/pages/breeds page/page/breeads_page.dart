import 'package:flutter/material.dart';

class BreadsPage extends StatelessWidget {
  const BreadsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}